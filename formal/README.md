# Executable Lane V1 models

See `../docs/FORMAL_VERIFICATION.md` and the Turn 13 report.

The default executed backend is the standard-library exact-state explorer plus
the explicitly scoped C11-RA handoff graph checker. Spin/GenMC inputs are supplied
for external execution; no absent-tool success is inferred.
